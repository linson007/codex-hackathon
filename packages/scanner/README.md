# @contextos/scanner

Purpose: heuristic scanner for Spring-style Java repositories, SQL migrations, config files, Kafka topics, Feign clients, controllers, services, repositories, entities, endpoints, and tables.

Entry point: `src/index.ts`

Development:

```bash
npm test -- packages/scanner/test/scanner.test.ts
npm run build
```
