# @contextos/cli

Purpose: command-line workflow for creating, indexing, querying, documenting, and serving ContextOS knowledge bases.

Entry point: `src/index.ts`

Development:

```bash
npm run contextos -- --help
./bin/contextos.js --help
```

The root `contextos` bin delegates to this workspace through `tsx`.
